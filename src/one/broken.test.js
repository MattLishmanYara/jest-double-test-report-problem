describe('Broken test suite', () => {
    it('should pass 1', () => {
        expect(1).toBe(1);
    });
    it('should pass 2', () => {
        expect(1).toBe(1);
    });
    it('should pass 3', () => {
        expect(1).toBe(1);
    });
    it('should pass 4', () => {
        expect(1).toBe(1);
    });
    it('should pass 5', () => {
        expect(1).toBe(1);
    });
    it('should pass 6', () => {
        expect(1).toBe(1);
    });
    it('should pass 7', () => {
        expect(1).toBe(1);
    });
    it('should pass 8', () => {
        expect(1).toBe(1);
    });
    it('should pass 9', () => {
        expect(1).toBe(1);
    });
    it('should pass 10', () => {
        expect(1).toBe(1);
    });
    it('should pass 11', () => {
        expect(1).toBe(1);
    });
    it('should pass 12', () => {
        expect(1).toBe(1);
    });
    it('should pass 13', () => {
        expect(1).toBe(1);
    });
    it('should pass 14', () => {
        expect(1).toBe(1);
    });
    it('should pass 15', () => {
        expect(1).toBe(1);
    });
    it('should pass 16', () => {
        expect(1).toBe(1);
    });
    it('should pass 17', () => {
        expect(1).toBe(1);
    });
    it('should pass 18', () => {
        expect(1).toBe(1);
    });
    it('should pass 19', () => {
        expect(1).toBe(1);
    });
    it('should pass 20', () => {
        expect(1).toBe(1);
    });
    it('should pass 21', () => {
        expect(1).toBe(1);
    });
    it('should pass 22', () => {
        expect(1).toBe(1);
    });
    it('should pass 23', () => {
        expect(1).toBe(1);
    });
    it('should pass 24', () => {
        expect(1).toBe(1);
    });
    it('should pass 25', () => {
        expect(1).toBe(1);
    });
    it('should pass 26', () => {
        expect(1).toBe(1);
    });
    it('should pass 27', () => {
        expect(1).toBe(1);
    });
    it('should pass 28', () => {
        expect(1).toBe(1);
    });
    it('should pass 29', () => {
        expect(1).toBe(1);
    });
    it('should pass 30', () => {
        expect(1).toBe(1);
    });
});
